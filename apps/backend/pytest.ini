[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
addopts =
    -q
    --strict-markers
markers =
    integration: needs Postgres (and Redis for AS cache paths)
